[pytest]
testpaths = tests
markers =
    slow: end-to-end smoke run of a real falsification script (actually invokes the
          simulator with a tiny budget). Skipped by default; run with `-m slow` or
          `--run-slow`.
addopts = -ra
